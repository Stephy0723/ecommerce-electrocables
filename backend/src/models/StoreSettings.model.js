// Modelo preparado: StoreSettings
// Reemplazar este esquema mock por Mongoose, Prisma o Sequelize cuando se conecte la base de datos.
export const StoreSettingsModel = {
  name: 'StoreSettings',
  fields: {},
};
