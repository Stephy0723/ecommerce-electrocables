// Preparado para MongoDB o PostgreSQL.
// MongoDB futuro: importar mongoose y conectar con env.DATABASE_URL.
// PostgreSQL futuro: crear pool con pg/Prisma/Sequelize.
export async function connectDatabase(){
  console.log('DB no conectada todavía. Backend funcionando en modo preparado/mock.');
}
